// # Catalog QA Index — Anchor for Agents
//
// Logline: Break-test drivers that ship with the catalog tree.
//
// Setup: Each proof is a hostile test for the published index. If a driver
//        fails, the catalog fails to release. Do not import package payloads.
//
// Beats:
//   1. catalog_proof.oo — hello, echo, and sqlite lines, sha matches manifest.
//   2. sign_index.oo — re-sign index.sig after `opm bot .` rewrites index.
//   3. suite.oo typechecks every driver twice (local entry; CI wiring pending).

import "catalog_proof.oo";
import "sign_index.oo";
import "suite.oo";
