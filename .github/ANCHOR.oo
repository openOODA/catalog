// # Catalog GitHub wiring
//
// Logline: GitHub auto-release and Pages deploy. Not product code.
//
// Setup: Outside the package folders. Version is the git tag (RFC-0006).
//
// Beats:
//   1. workflows/ci.yml gates the catalog file, then auto-release.
//   2. workflows/deploy.yml publishes this tree to GitHub Pages.
//   3. qa/suite.oo typechecks every driver twice (local entry).
//   4. No VERSION file.

import "workflows/ANCHOR.oo";
